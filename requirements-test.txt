# Minimal dependencies for running CI tests (no vnstock/heavy packages)
# Used by GitHub Actions CI — fast install, no external API keys needed

# Core pipeline deps needed by tests
SQLAlchemy==2.0.30
psycopg2-binary==2.9.9
redis==5.0.4
paho-mqtt>=2.0
python-dotenv==1.0.1
pydantic-settings==2.2.1
loguru==0.7.2
tenacity==8.2.3
requests>=2.31
APScheduler>=3.10.4,<4.0
pandas>=1.5.3

# Test runner + linter
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4
